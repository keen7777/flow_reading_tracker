import { TextDisplayDirective } from './text-display.directive';

describe('TextDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new TextDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
